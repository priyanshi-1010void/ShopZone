# Flask Secret Key
SECRET_KEY=shopzone_super_secret_key_bca_sem3_2026

# Database Connection (MySQL / PostgreSQL / SQLite)
# For local MySQL:
DATABASE_URL=mysql+pymysql://root:abc123@localhost/ecommerce_db

# Or for local SQLite (zero-config, no MySQL server required):
# USE_SQLITE=true

# For Cloud PostgreSQL (Render / Supabase / Neon / Railway):
# DATABASE_URL=postgresql://username:password@host:5432/database_name
